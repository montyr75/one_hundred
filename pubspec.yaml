name: one_hundred
description: A web dice game that uses AngularDart Components
version: 0.0.1

environment:
  sdk: '>=2.0.0-dev.61.0 <2.0.0'

dependencies:
  angular: 5.0.0-alpha+15
  angular_components: 0.9.0-alpha+15

dev_dependencies:
  build_runner: ^0.8.8
  build_web_compilers: ^0.4.0
